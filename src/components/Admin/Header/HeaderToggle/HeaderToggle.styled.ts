import styled from "styled-components";

const HeaderToggle = styled.div`
  display: flex;
  flex-direction: column;
`;

const ToggleBtn = styled.div`
  padding: var(--s-4);
  cursor: pointer;
`;

export { HeaderToggle, ToggleBtn };
