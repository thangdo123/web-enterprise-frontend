import styled from "styled-components";

const LeftHeaderList = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: var(--s-4);
  width: 100%;
`;

export { LeftHeaderList };
