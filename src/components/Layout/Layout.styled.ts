import styled from "styled-components";

const LayoutContainer = styled.div`
  display: flex;
  flex-direction: row;
  gap: var(--s-4);
  height: 100%;
  min-height: 100vh;
`;

export { LayoutContainer };
